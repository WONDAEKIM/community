void main() {
  //Variable can be declared without initialization.
  late final String name;
  name = "Sogang University";
  //Widely used when we handle API. Only one initialization is allowed for this kind of situation.
  print(name);
}
